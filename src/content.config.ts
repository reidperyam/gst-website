/**
 * Astro Content Collections configuration.
 *
 * Required by Astro 6+ even when no collections are defined — otherwise
 * the dev server logs a "Content config not loaded" warning on startup.
 *
 * Currently empty. Phase 8 of PLATFORM_HARDENING_V1 will migrate the
 * regulatory-map JSON files from `src/data/regulatory-map/` into an
 * Astro content collection, at which point that collection schema will
 * be defined here.
 */

export const collections = {};
