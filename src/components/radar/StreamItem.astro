---
/**
 * Stream item — Tier 1 automated content.
 * Compact: category dot, headline, source, date.
 */
import { CATEGORIES } from '../../lib/inoreader/transform';
import type { RadarStreamItem } from '../../lib/inoreader/types';

interface Props {
  item: RadarStreamItem;
}

const { item } = Astro.props;
const cat = CATEGORIES[item.category];
const pubDate = new Date(item.publishedAt);
const dateStr = pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
---

<article class="stream-item" data-category={item.category}>
  <div class="stream-item__indicator">
    {cat && <span class="dot" style={`background: ${cat.color}`}></span>}
  </div>
  <div class="stream-item__content">
    <a href={item.url} target="_blank" rel="noopener noreferrer" class="stream-item__title">
      {item.title}
    </a>
    <div class="stream-item__meta">
      <span class="source">{item.source}</span>
      <span class="date">{dateStr}</span>
    </div>
  </div>
</article>

<style>
  .stream-item {
    display: flex;
    gap: var(--spacing-md);
    padding: var(--spacing-md) 0;
    border-bottom: 1px solid var(--border-light);
  }

  .stream-item__indicator {
    padding-top: 0.375rem;
    flex-shrink: 0;
  }

  .dot {
    display: block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
  }

  .stream-item__title {
    font-size: var(--text-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--text-light-primary);
    text-decoration: none;
    line-height: 1.4;
    display: block;
    margin-bottom: 0.25rem;
    transition: color var(--transition-fast);
  }

  .stream-item__title:hover {
    color: var(--color-primary);
  }

  .stream-item__meta {
    font-size: var(--text-xs);
    color: var(--text-light-muted);
  }

  .stream-item__meta .source::after {
    content: "·";
    margin: 0 var(--spacing-xs);
  }

  :global(html.dark-theme) .stream-item {
    border-bottom-color: rgba(255, 255, 255, 0.05);
  }
</style>
