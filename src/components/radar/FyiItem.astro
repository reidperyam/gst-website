---
/**
 * FYI item — Tier 2 curated content.
 * Collapsed by default; expands to show summary, highlight, and GST Take.
 * Uses native <details>/<summary> for zero-JS, accessible expand/collapse.
 */
import { CATEGORIES } from '../../lib/inoreader/transform';
import type { RadarFyiItem } from '../../lib/inoreader/types';
import DeltaIcon from '../DeltaIcon.astro';

interface Props {
  item: RadarFyiItem;
}

const { item } = Astro.props;
const catColor = CATEGORIES[item.category]?.color ?? '#9B59B6';
const pubDate = new Date(item.publishedAt);
const dateStr = pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
---

<article class="fyi-item" data-category={item.category} style={`--cat-color: ${catColor}`}>
  <details class="fyi-item__details">
    <summary class="fyi-item__header">
      <div class="fyi-item__meta">
        <span class="editors-pick-tag">Editor's Pick</span>
        <span class="source">{item.source}</span>
        <span class="date">{dateStr}</span>
      </div>
      <div class="fyi-item__headline">
        <DeltaIcon size={14} class="fyi-item__toggle delta-chevron" />
        <h3 class="fyi-item__title">
          <a
            href={item.url}
            target="_blank"
            rel="noopener noreferrer"
            onclick="arguments[0].stopPropagation();"
          >
            {item.title}
          </a>
        </h3>
      </div>
    </summary>
    <div class="fyi-item__body">
      {item.summary && <p class="fyi-item__summary">{item.summary}</p>}

      {
        item.highlightedText && (
          <div class="fyi-item__highlight">
            <span class="brutal-content-label" style="color: var(--cat-color);">
              Highlighted
            </span>
            <blockquote>{item.highlightedText}</blockquote>
          </div>
        )
      }

      {
        item.highlightedText && item.gstTake && (
          <div class="fyi-item__nested-take">
            <span class="brutal-content-label">Δ GST Take</span>
            <p>{item.gstTake}</p>
          </div>
        )
      }

      {
        !item.highlightedText && item.gstTake && (
          <div class="fyi-item__take">
            <span class="brutal-content-label">Δ GST Take</span>
            <p>{item.gstTake}</p>
          </div>
        )
      }
    </div>
  </details>
</article>

<style>
  .fyi-item {
    padding: var(--spacing-md) 0;
    border-bottom: 1px solid light-dark(var(--border-light), var(--border-dark-default));
  }

  /* Reset native details/summary styling */
  .fyi-item__details summary {
    list-style: none;
    cursor: pointer;
  }

  .fyi-item__details summary::-webkit-details-marker {
    display: none;
  }

  .fyi-item__header {
    display: block;
  }

  .fyi-item__meta {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    font-size: var(--text-xs);
    font-family: var(--font-family-mono);
    color: var(--text-muted);
    margin-bottom: var(--spacing-xs);
  }

  .fyi-item__headline {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
  }

  .fyi-item__title {
    font-size: var(--text-base);
    font-family: var(--font-family-mono);
    font-weight: var(--font-weight-semibold);
    line-height: 1.4;
    margin: 0;
    flex: 1;
  }

  .fyi-item__title a {
    color: var(--text-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .fyi-item__title a:hover {
    color: var(--color-primary);
  }

  .fyi-item__toggle {
    margin-top: -0.125rem;
  }

  /* Expanded body */
  .fyi-item__body {
    padding-top: var(--spacing-sm);
    padding-left: var(--spacing-xl);
  }

  .fyi-item__summary {
    font-size: var(--text-sm);
    font-family: var(--font-family-mono);
    color: var(--text-secondary);
    line-height: 1.5;
    margin-bottom: var(--spacing-sm);
  }

  .fyi-item__highlight {
    border-left: 3px solid var(--cat-color);
    padding: var(--spacing-sm) var(--spacing-md);
    margin: var(--spacing-sm) 0;
  }

  .fyi-item__highlight blockquote {
    font-size: var(--text-sm);
    color: var(--text-primary);
    line-height: 1.6;
    margin: 0;
  }

  .fyi-item__take {
    background: var(--accent-light-bg);
    border-left: 3px solid var(--color-primary);
    padding: var(--spacing-sm) var(--spacing-md);
    margin-top: var(--spacing-sm);
  }

  .fyi-item__nested-take {
    margin-top: var(--spacing-md);
    margin-left: var(--spacing-md);
    padding-top: var(--spacing-sm);
    padding-left: var(--spacing-md);
    border-top: 1px solid light-dark(var(--border-light), var(--border-dark-default));
  }

  .fyi-item__nested-take p {
    font-size: var(--text-sm);
    color: var(--text-primary);
    line-height: 1.6;
    margin: 0;
  }

  .fyi-item__take p {
    font-size: var(--text-sm);
    color: var(--text-primary);
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 480px) {
    .fyi-item__meta {
      flex-wrap: wrap;
    }

    .fyi-item__title {
      font-size: var(--text-sm);
    }
  }
</style>
