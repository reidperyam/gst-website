name: PR Preview Deployment

on:
  pull_request:
    branches: [master]

jobs:
  preview-deployment:
    name: Vercel Preview Deployment
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Vercel deployment info
        run: |
          echo "✅ Preview deployment will be created by Vercel"
          echo "Waiting for test suite (test.yml) to complete..."
          echo "Review results in GitHub checks before merging"
